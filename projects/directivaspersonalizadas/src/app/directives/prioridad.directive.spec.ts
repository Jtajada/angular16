import { PrioridadDirective } from './prioridad.directive';

describe('PrioridadDirective', () => {
  it('should create an instance', () => {
    const directive = new PrioridadDirective();
    expect(directive).toBeTruthy();
  });
});

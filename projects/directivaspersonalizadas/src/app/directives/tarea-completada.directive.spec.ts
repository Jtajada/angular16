import { TareaCompletadaDirective } from './tarea-completada.directive';

describe('TareaCompletadaDirective', () => {
  it('should create an instance', () => {
    const directive = new TareaCompletadaDirective();
    expect(directive).toBeTruthy();
  });
});
